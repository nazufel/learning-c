#include <stdio.h>
#include <stdlib.h>

int main() {

    // integer var
    int age = 30;

    // double var
    double gpa = 3.7;
    
    // store a single character, must use single quotes
    char grade = 'A';

    // store a string of characters in an array, use double quotes
    char class[] = "Compute Science";

    return 0;
}